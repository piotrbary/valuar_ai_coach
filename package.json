{
  "name": "valuar-ai-coach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reads Strava training data as a foundation for an AI coaching app.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "auth": "tsx src/cli.ts auth",
    "activities": "tsx src/cli.ts activities",
    "web": "tsx src/server.ts",
    "mcp": "tsx src/mcp-server.ts",
    "hosted": "tsx src/hosted/server.ts",
    "generate-icons": "node scripts/generate-icons.mjs",
    "validate:plugin": "node scripts/validate-plugin.mjs",
    "test": "npm run typecheck && npm run validate:plugin",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "open": "^10.1.0",
    "zod": "^4.5.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^20.14.10",
    "pngjs": "^7.0.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  }
}
